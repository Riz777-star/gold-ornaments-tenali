const detail=document.querySelector('#detail-dialog'),enquiry=document.querySelector('#enquiry-dialog');
document.querySelectorAll('.close-dialog').forEach(b=>b.addEventListener('click',()=>b.closest('dialog').close()));[detail,enquiry].forEach(d=>d.addEventListener('click',e=>{if(e.target===d){const r=d.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)d.close();}}));
const form=document.querySelector('#enquiry-form');function updateSummary(){const purity=form.querySelector('input:checked').value;document.querySelector('.enquiry-summary').textContent=document.querySelector('#piece').value+' · '+purity+' · '+document.querySelector('#occasion').value;}form.addEventListener('change',updateSummary);updateSummary();
form.addEventListener('submit',event=>{event.preventDefault();const purity=form.querySelector('input:checked').value;const notes=document.querySelector('#details').value.trim();document.querySelector('#prepared-message').textContent=`Hello, I would like to enquire about:\n\nPiece: ${document.querySelector('#piece').value}\nGold preference: ${purity}\nOccasion: ${document.querySelector('#occasion').value}${notes?'\nDetails: '+notes:''}\n\nPlease let me know suitable options and pricing. Thank you!`;document.querySelector('#copy-status').textContent='';document.querySelector('#copy-message').textContent='Copy enquiry';enquiry.showModal();});
document.querySelector('#copy-message').addEventListener('click',async()=>{try{await navigator.clipboard.writeText(document.querySelector('#prepared-message').textContent);document.querySelector('#copy-status').textContent='Copied. Paste it into your preferred messaging app.';document.querySelector('#copy-message').textContent='Enquiry copied ✓';}catch{const range=document.createRange();range.selectNodeContents(document.querySelector('#prepared-message'));const selection=window.getSelection();selection.removeAllRanges();selection.addRange(range);document.querySelector('#copy-status').textContent='Select and copy the highlighted enquiry using your device’s copy option.';}});
const menu=document.querySelector('.menu'),nav=document.querySelector('nav');menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open));menu.setAttribute('aria-label',open?'Close menu':'Open menu');menu.textContent=open?'×':'☰';});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu.setAttribute('aria-expanded','false');menu.setAttribute('aria-label','Open menu');menu.textContent='☰';}));
if('IntersectionObserver'in window&&!matchMedia('(prefers-reduced-motion: reduce)').matches){document.documentElement.classList.add('js-motion');const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target);}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));}
